diff --git a/src/adapters.rs b/src/adapters.rs
index 236f689..3517370 100644
--- a/src/adapters.rs
+++ b/src/adapters.rs
@@ -44,6 +44,10 @@ pub trait SyntaxHighlighterAdapter: Send + Sync {
 
 /// The struct passed to the [`HeadingAdapter`] for custom heading implementations.
 #[derive(Clone, Debug)]
+#[cfg_attr(
+    feature = "serde",
+    derive(serde::Serialize, serde::Deserialize)
+)]
 pub struct HeadingMeta {
     /// The level of the heading; from 1 to 6 for ATX headings, 1 or 2 for setext headings.
     pub level: u8,
